# Elasticsearch with exposed port and custom heap size
elasticsearch:
  image: elasticsearch:2.0.0
  command:
    - "elasticsearch"
    - "--network.host"
    - "0.0.0.0"
  environment:
    ES_HEAP_SIZE: 1G

logstash:
  image: quay.io/cogniteev/oncrawl-elk-logstash
  links:
    - elasticsearch
  volumes:
    - ./logs:/logs

# Kibana index initializer
kibana-config:
  image: quay.io/cogniteev/oncrawl-elk-kibana-config
  links:
    - elasticsearch

# Kibana with modified entry point to wait for index initialization
kibana:
  image: cogniteev/oncrawl-elk-kibana
  links:
    - elasticsearch

web-client:
  image: quay.io/cogniteev/oncrawl-elk-web-client
  links:
    - kibana
  ports:
    - "9000:80"
